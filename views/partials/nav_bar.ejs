<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container-fluid">
        <!-- Website Name -->
        <a class="navbar-brand fw-bold custom-brand" href="/">My Book Tracker</a>

        <!-- Toggler for Mobile View -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <!-- Search Form -->
                <form class="d-flex custom-search-form me-3" action="/search" method="GET">
                    <input
                        class="form-control me-2 custom-search-input"
                        type="search"
                        placeholder="Search..."
                        name="query"
                        aria-label="Search"
                    />
                    <button class="btn btn-primary custom-search-btn" type="submit">Search</button>
                </form>

                <!-- Rating and Date Read Filters -->
                <form class="d-flex align-items-center" action="/filter" method="GET">
                    <!-- Rating Filter -->
                    <div class="me-3">
                        <label for="filterRating" class="form-label me-2">Rating:</label>
                        <select
                            id="filterRating"
                            class="form-select"
                            name="rating"
                        >
                            <option value="">All</option>
                            <option value="5">5</option>
                            <option value="4">4</option>
                            <option value="3">3</option>
                            <option value="2">2</option>
                            <option value="1">1</option>
                        </select>
                    </div>

                    <!-- Date Read Filter -->
                    <div class="me-3">
                        <label for="filterDate" class="form-label me-2">Date Read:</label>
                        <input
                            id="filterDate"
                            class="form-control"
                            type="date"
                            name="date_read"
                        />
                    </div>

                    <!-- Filter Button -->
                    <button class="btn btn-secondary custom-filter-btn" type="submit">Apply</button>
                </form>
            </ul>
        </div>
    </div>
</nav>
